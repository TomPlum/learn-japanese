import {render} from "@testing-library/react";
import KanaDisplay from "../../../components/ui/display/KanaDisplay";

describe("Kana Tile", () => {
   it("Should render the passed kana property", () => {

   });
});